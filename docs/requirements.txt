# Dependencies for building the documentation site (used by the docs CI workflow).
#   pip install -r docs/requirements.txt
#   mkdocs serve        # preview locally at http://127.0.0.1:8000
#   mkdocs build        # render the static site into ./site
mkdocs-material>=9.5
