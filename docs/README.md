# docs/

Put the README's lead screenshot here as **`screenshot.png`**.

Suggested shot: the **Calculations** tab with a job mid-run (live state colours
visible), or the **Molecules** tab showing a generated structure in the depiction
pane. Roughly 1200 px wide reads well on GitHub.

The main `README.md` references it as `docs/screenshot.png`.
